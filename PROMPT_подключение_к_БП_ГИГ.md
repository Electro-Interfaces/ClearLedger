# Промпт для агента ClearLedger: подключение к БП ГИГ + получение данных

Скопировать и вставить в новую сессию Claude Code в директории `D:\Users\magsp\Ledger\`.

---

## Контекст

**БП ГИГ** — конфигурация **«1С:Бухгалтерия предприятия 3.0»** (ред. 3.0.194.18) для **ООО «ГАЗИНВЕСТГРУПП» (ИНН 7839440090, КПП 780401001)**. Это первый клиент платформы ClearLedger.

Установлено расширение **`TradeLedger.cfe`** v6.0+ — реализует трёхконтурный приём документов с АЗС:
- **Нефтепродукты** — через STS API (`pos.autooplata.ru/tms`) → топливные ОРП, ПТУ ТТН
- **Сопутка** — через JSON-пакеты из ЦБ ЭЛСИ.АЗК в каталоге OwnCloud → ПТУ, ОРП, КорректировкаПоступления
- **Общепит** — аналогично сопутке → ОПЗС, СписаниеТоваров, ВозвратТоваровОтПокупателя

ClearLedger должен **читать** из БП ГИГ:
- НСИ (Контрагенты, Номенклатура, Договоры, Склады, Организации) для сверки и нормализованной БД
- Документы и проводки за период для аудита/сверки/отчётности
- Остатки на счетах учёта (41.02 / 60.01 / 19.03 / 90.01.1 / 90.03 / 68.02 / 62.Р / 50.01 / 57.03 / 44.01)
- Регистры расширения `TL_СоответствиеИсточников`, `TL_СверкаПакета`, `TL_ОшибкиЗагрузки` — для понимания статуса миграции через TL-канал

---

## Реквизиты подключения

### Боевая (production) — основной источник

| Параметр | Значение |
|---|---|
| **Тип публикации** | Apache + HTTP, типовая публикация 1С |
| **Адрес OData** | `http://192.168.40.31/acc/odata/standard.odata/` |
| **Имя информационной базы** | `GIG` (в SQL-кластере) |
| **Web-публикация** | `acc` (alias в Apache) |
| **Логин** | `Администратор` (или сервисный пользователь, согласовать с МАГ) |
| **Пароль** | в `D:\Users\magsp\ai-base\access\` (запросить у МАГ) |
| **Сетевой доступ** | VPN ElsyPlus или локально из офиса. См. `D:\Users\magsp\ai-base\vpn\` |

⚠ **Боевую трогать только на чтение**. Любая запись — только после явного согласования с МАГ и Сидоренко (HOS).

### Стенд (для разработки и smoke)

| Параметр | Значение |
|---|---|
| **Файловая БД** | `D:\Users\magsp\GIG Base2` (используется через COM, не OData) |
| **Платформа** | 1С 8.3.27.2074 |
| **Учётная запись** | `Гайворонская Татьяна` / `12345` (на стенде упрощённая) |
| **Опубликована через Apache** | Нет (файловая БД, для OData нужно опубликовать отдельно) |

Если нужен OData на стенде — потребуется публикация через `webinst.exe` (есть `web-publish` skill в `C:\Users\magsp\.claude\skills\`).

---

## Механизмы получения данных

### Механизм 1: OData REST (рекомендуется для чтения)

**Стандартный механизм 1С 8.3**, доступен из коробки после публикации через Apache. URL формируется из имени метаданного объекта.

```
GET http://192.168.40.31/acc/odata/standard.odata/Catalog_Контрагенты?$format=json
```

Auth: HTTP Basic (`Authorization: Basic <base64(login:password)>`).

**Особенности БП 3.0 / OData**:
- **Кириллические имена объектов** в URL — URL-encode обязателен. `Catalog_Контрагенты` → `Catalog_%D0%9A%D0%BE%D0%BD%D1%82%D1%80%D0%B0%D0%B3%D0%B5%D0%BD%D1%82%D1%8B`.
- Английские псевдонимы метаданных не работают (кириллица as-is).
- `$select`, `$filter`, `$orderby`, `$top`, `$skip` поддерживаются.
- Регистр сведений: `InformationRegister_<Имя>` (срез последних — `_SliceLast`).
- Регистр накопления: `AccumulationRegister_<Имя>` (остатки — `_Balance`, обороты — `_Turnovers`).
- Регистр бухгалтерии: `AccountingRegister_<Имя>` (проводки — без виртуальной таблицы; остатки — `_BalanceAndTurnovers`).
- Кириллические **значения** в `$filter` — тоже URL-encode + одинарные кавычки: `?$filter=ИНН eq '7839440090'`.

**Ключевые сущности для ClearLedger**:

| OData URL | Что |
|---|---|
| `Catalog_Контрагенты` | Контрагенты (фильтр по ИНН) |
| `Catalog_Номенклатура` | Номенклатура (7000+ позиций) |
| `Catalog_ДоговорыКонтрагентов` | Договоры |
| `Catalog_Склады` | Склады/АЗС |
| `Catalog_Организации` | Юр.лица |
| `Document_ПоступлениеТоваровУслуг` | ПТУ от поставщиков |
| `Document_ОтчетОРозничныхПродажах` | ОРП (розничные продажи) |
| `Document_ОтчетПроизводстваЗаСмену` | ОПЗС (общепит) |
| `Document_СписаниеТоваров` | Списания |
| `Document_ПриходныйКассовыйОрдер` | ПКО |
| `Document_ПеремещениеТоваров` | Перемещения между складами |
| `Document_КорректировкаПоступления` | Корректировки (используется как возврат поставщику) |
| `InformationRegister_ХозрасчетныйПериодическийРегистрСведений` | Параметры учёта (если есть) |
| `AccountingRegister_Хозрасчетный` | Все проводки. Поля: `Период`, `Регистратор`, `Счет`/`СчетКт`/`СчетДт`, `Сумма`, `Субконто*` |
| `AccumulationRegister_ПартииТоваровНаСкладах_Balance` | Остатки партий |

**Регистры расширения `TradeLedger.cfe`** (имена с префиксом `TL_`, может потребоваться отдельная активация публикации расширения):

| OData URL | Что |
|---|---|
| `InformationRegister_TL_СоответствиеИсточников` | Маппинг UUID документов ЦБ → ссылки в БП. Ключевой для сверки |
| `InformationRegister_TL_СверкаПакета` | Хеши обработанных пакетов (идемпотентность) |
| `InformationRegister_TL_ОшибкиЗагрузки` | Журнал ошибок загрузки (см. раздел «Особенности» ниже) |
| `InformationRegister_TL_СтатусыЗагрузки` | Связь TL-ключей с документами БП |
| `InformationRegister_TL_МаппингОплат` | Маппинг pay_type STS → каналы (cards/online/retail_cash/...) |

### Механизм 2: COM-коннектор V83.COMConnector (только локально)

Если ClearLedger запущен на той же машине что и БД (или имеет прямой SQL-доступ) — `V83.COMConnector` даёт **полный** доступ через язык 1С запросов. Это **только Windows и только Python 3.x 32-bit**.

```python
import win32com.client
con = win32com.client.Dispatch("V83.COMConnector")
ib = con.Connect('File="D:\\Users\\magsp\\GIG Base2";Usr="Гайворонская Татьяна";Pwd="12345";')
# или для боевой через сервер:
# ib = con.Connect('Srvr="192.168.40.31:1541";Ref="GIG";Usr="...";Pwd="..."')

q = ib.NewObject("Запрос")
q.Текст = "ВЫБРАТЬ ПЕРВЫЕ 10 К.Наименование, К.ИНН ИЗ Справочник.Контрагенты КАК К"
sel = q.Выполнить().Выбрать()
while sel.Следующий():
    print(sel.Наименование, sel.ИНН)
```

**Подводные камни pywin32** (см. готовые helper'ы в существующих probe-скриптах `D:\Users\magsp\ELSYPLUS\NefteUchet\scripts\`):
- Поля типа Variant приходят как `bound-method` → нужен универсальный геттер `_val()/num()/s()`. Готовый шаблон — `probe_full_audit.py`.
- Перечисления (`ВидОперации`, `СтавкаНДС`) возвращаются как `<COMObject>` — использовать `ПРЕДСТАВЛЕНИЕ()` в SELECT.
- Массивы для `В (&Массив)` через `ib.NewObject("Array")` + `.Add()`.
- `СГРУППИРОВАТЬ ПО ПРЕДСТАВЛЕНИЕ()` падает — группировать по enum-ссылке.
- `РежимЗаписиДокумента` — enum платформы, не число.

### Механизм 3: HTTP-сервис расширения (если нужны кастомные эндпоинты)

В `TradeLedger.cfe` уже есть инфраструктура HTTP-клиента (`TL_HTTPКлиентЦБ.bsl`). При необходимости можно добавить **HTTP-сервис** `CLR_API` для отдачи нормализованных данных конкретно под ClearLedger — это даст RESTful endpoint без накладных расходов OData.

**Делать только если OData не покрывает требования** (нестандартная агрегация, большие выгрузки с фильтрами, кэширование).

### Механизм 4: Push через каталог-обмен (обратный направление)

Уже работающий механизм в TradeLedger: ЦБ ЭЛСИ.АЗК выгружает JSON-пакеты в `C:\TL_BP_Export\` (или OwnCloud), БП ГИГ их читает через `TL_HTTPКлиентЦБ.ПолучитьСписокНовыхПакетов`.

Для ClearLedger **обратный** механизм возможен: ClearLedger складывает нормализованные пакеты в каталог, БП ГИГ их тянет. **Но это противоречит идеологии ClearLedger** (memory `project_clearledger_ideology_v3` — «1С-расширение ТЯНЕТ из ClearLedger», не наоборот, и не push-в-БП). Сначала уточнить с МАГ.

---

## Особенности БП 3.0 / TradeLedger.cfe (критичные правила)

### Розничная модель ОРП БП 3.0

Для `Документ.ОтчетОРозничныхПродажах` с `ВидОперации=ОтчетККМОПродажах`:
- `Кт 90.01.1` = `СуммаДокумента` (вся выручка **С НДС**)
- `Кт 68.02` = `round(90.01.1 × 22/122, 2)` (выделение НДС)
- `Σ Дт 62.Р` = `Σ Кт 62.Р` = `СуммаДокумента`

**НЕ применять** формулу `Σ(90.01.1+90.03) == СуммаДок` к розничному ОРП — это даёт ложное «расхождение 18%». Это формула B2B Реализации с `СуммаВключаетНДС=Ложь`, к рознице не относится. В ОФР строка 2110 «Выручка» = `Кт 90.01 − Дт 90.03` (чистая без НДС).

Memory: `project_orp_retail_vat_model`.

### Журнал-лог `TL_ОшибкиЗагрузки`

Регистр работает как **история событий**, не как «текущие открытые проблемы». После исправления записи остаются. Прежде чем считать «ошибки» — сверить `ИсточникUUID` записи с `TL_СоответствиеИсточников`: если карточка уже создана (запись соответствия есть) — ошибка историческая.

На 23.05.2026 на стенде: из 177 НСИ-ошибок реально открыто **9**, остальные 168 — исторические.

Memory: `project_tl_oshibki_zhurnal_pattern`.

### Идемпотентность пакетов

`TL_СверкаПакета` хранит `ИдентификаторПакета` (UUID) + `ХешПакета` (sha256 содержимого). Один и тот же пакет дважды не загружается — повторная попытка возвращает «уже загружен».

### Структура каталогов на сервере 1С

- `D:\Users\magsp\GIG Base2` — локальный стенд для разработки
- `192.168.40.31\GIG` — боевая БП ГИГ через сервер
- `C:\TL_BP_Export\` — каталог-обмен ЦБ→БП (92 пакета на стенде)

---

## Что НЕ делать

1. **Не писать в боевую через OData** без согласования с МАГ (всегда читать в первую очередь, в записи — только через утверждённые механизмы).
2. **Не загружать СОТНЯМИ запросов** на боевую без `$top` — нагрузит сервер.
3. **Не использовать пагинацию OData бездумно** — у некоторых сущностей (Хозрасчетный регистр) миллионы записей.
4. **Не применять формулу `Σ(90.01.1+90.03) = СуммаДок` к розничным ОРП** (см. выше).
5. **Не считать `TL_ОшибкиЗагрузки` как «открытые проблемы»** — это журнал-лог.
6. **Не трогать `Catalog.Пользователи`, ParametрыСеанса, Constants** через OData — это область администрирования.

---

## Готовые скрипты для референса

В этой задаче можно опираться на готовые probe-скрипты которые уже работают с БП ГИГ через COM:

| Скрипт | Что делает |
|---|---|
| `D:\Users\magsp\ELSYPLUS\NefteUchet\scripts\probe_full_audit.py` | Полный 8-разделовой аудит TradeLedger в БП ГИГ — целостность, проводки, контроль сумм (розничная модель), полнота смен, аномалии, ошибки с фильтром Открыто/Исправлено |
| `NefteUchet/scripts/probe_souputka_obshchepit_state.py` | Сводка документов сопутки/общепита за период (ПТУ/ОРП/ОПЗС) + примеры по складу |
| `NefteUchet/scripts/probe_orp_vat_diagnosis.py` | Разбор НДС-проводок одной ОРП — реквизиты, ТЧ, проводки 90.01.1/68.02 |
| `NefteUchet/scripts/_tl_config.py` | Шаблон подключения через COM с переменными окружения |

---

## Что в результате должно быть готово

1. **Документ архитектуры подключения** — какой механизм выбран (OData / COM / HTTP-сервис), почему, какие endpoint'ы потребляются.
2. **Working код** — минимальный пример: подключение → читаем 10 контрагентов → сверяем с локальной нормализованной БД ClearLedger.
3. **Конфиг** в `D:\Users\magsp\Ledger\config\1c_connection.yml` (без секретов, секреты в `.env` или `secrets.json` gitignored).
4. **README** для разработчика — как получить доступы, как настроить локально, как тестировать.

---

## Связанные memory (читать перед стартом)

В директории `C:\Users\magsp\.claude\projects\D--Users-magsp-OneDrive-GIG-Ledger\memory\` — все детали БП ГИГ и TradeLedger.cfe.

- `project_clearledger_ideology_v3` — идеология ClearLedger (рабочее место менеджеров, нормализованная БД, тянущая модель)
- `project_orp_retail_vat_model` — розничная модель ОРП БП 3.0
- `project_tl_oshibki_zhurnal_pattern` — журнал-лог ошибок
- `project_cb_packets_audit_23052026` — состояние ЦБ-выгрузки (источник данных для сопутки/общепита)
- `project_tradeledger_prod_settings_20260521` — эталон настроек TradeLedger на проде
- `project_rib_msn_not_in_exchange` — МСН-объекты не в РИБ ЦБ↔АЗС
- `project_v55_release`, `project_v56_release`, `project_v60_kickoff` — релизы TradeLedger

Skill `gig-1c` в `C:\Users\magsp\.claude\skills\gig-1c\SKILL.md` — 12 золотых правил работы с 1С под GIG Ledger.
