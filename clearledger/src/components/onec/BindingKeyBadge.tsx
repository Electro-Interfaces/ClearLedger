/**
 * Поясняющий бейдж активного ключа привязки станции/кассы к объекту.
 *
 * Привязка источника (STS/касса) к объекту учёта идёт по СОСТАВНОМУ ключу
 * [Склад, КодНС], а не по одному коду — иначе станции двоятся и путаются
 * (известная грабля перецепа дублей 208). Ключ критичен для корректности
 * проводок, поэтому его активное значение показывается прямо на поверхностях
 * коннекторов и маппинга, а не остаётся скрытым за «fingerprint»
 * (стандарт управления сложностью §4, §5, правило 3).
 */
import { Link2 } from 'lucide-react'
import { Badge } from '@/components/ui/badge'

export function BindingKeyBadge({ className }: { className?: string }) {
  return (
    <Badge
      variant="outline"
      title="Привязка источника к объекту — по составному ключу [Склад, КодНС], а не по одному коду. Иначе станции двоятся."
      className={`gap-1 font-normal border-zinc-600 text-zinc-400 ${className ?? ''}`}
    >
      <Link2 className="h-3 w-3" />
      Ключ привязки: Склад + КодНС
    </Badge>
  )
}
