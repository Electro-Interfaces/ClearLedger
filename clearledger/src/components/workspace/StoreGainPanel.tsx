/**
 * «Оприходования» раздела «Магазин».
 *
 * Единственный вид документа станции, у которого в центре не было своего места:
 * агент заводит оприходование (`gain`), пакет доезжает и раскладывается в
 * проекцию документов — но в разделе «Склад» его было не найти, только в общем
 * журнале «Документы». Постановка товара на учёт без накладной меняет остаток и
 * попадает в себестоимость, поэтому смотреть её надо там же, где списания.
 *
 * Реестра из ЦБ здесь нет намеренно: оприходование ведёт агент, у ЦБ ЭЛСИ.АЗК
 * такого потока в «Магазин» не приходит. Экран показывает ровно то, что есть, —
 * документы станций.
 */
import { StationDocsBlock } from './StationDocsBlock'

export function StoreGainPanel({ dateFrom, dateTo }: {
  dateFrom?: string; dateTo?: string
}) {
  return (
    <div className="p-6 space-y-4">
      <div>
        <h3 className="text-base font-semibold">Оприходования</h3>
        <p className="text-xs text-muted-foreground mt-0.5">
          Постановка товара на учёт без накладной: найденное при пересчёте, приход
          без документов, исправление, возврат неопознанного. Причина и автор — из
          документа станции.
        </p>
      </div>

      <StationDocsBlock kind="gain" dateFrom={dateFrom} dateTo={dateTo}
        title="Оприходования станции" />
    </div>
  )
}
