# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Deploy secrets
deploy/.env
deploy/data/
deploy/backups/
server/probe_*.py
server/uploads/

# Windows reserved-name junk (случайные редиректы в nul)
nul

# Локальные аудит-скрипты с секретами (.token) — не коммитить
undefined/

# Артефакты Playwright (скриншоты и трейсы падений)
test-results/
playwright-report/
# Сохранённая сессия e2e — содержит токен, не коммитить
e2e/.auth/

# Скриншоты проверок интерфейса — след работы, а не исходник
.qa/
