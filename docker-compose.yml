services:
  web:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    volumes:
      - storage:/data/storage
      - backups:/data/backups
    environment:
      - DATABASE_URL=postgresql+asyncpg://cl:${DB_PASS:-clearledger}@db:5432/clearledger
      - DATABASE_URL_SYNC=postgresql://cl:${DB_PASS:-clearledger}@db:5432/clearledger
      - STORAGE_ROOT=/data/storage
      - SECRET_KEY=${SECRET_KEY:-dev-secret-change-in-production}
      - CORS_ORIGINS=${CORS_ORIGINS:-http://localhost:3000,http://localhost:8080}
      - CLOUD_API_URL=${CLOUD_API_URL:-}
      - CLOUD_API_KEY=${CLOUD_API_KEY:-}
      - INSTANCE_ID=${INSTANCE_ID:-dev-local}
      - RATE_LIMIT=${RATE_LIMIT:-60/minute}
      - RATE_LIMIT_AUTH=${RATE_LIMIT_AUTH:-10/minute}
      # Сверки — внешние API (значения в host .env на сервере, НЕ в git)
      - TRADECORP_API_URL=${TRADECORP_API_URL:-}
      - TRADECORP_LOGIN=${TRADECORP_LOGIN:-}
      - TRADECORP_PASSWORD=${TRADECORP_PASSWORD:-}
      - TRADECORP_EMITENT_ID=${TRADECORP_EMITENT_ID:-15}
      - MSTO_API_URL=${MSTO_API_URL:-}
      - MSTO_USERNAME=${MSTO_USERNAME:-}
      - MSTO_PASSWORD=${MSTO_PASSWORD:-}
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped

  db:
    image: postgres:16-alpine
    volumes:
      - pgdata:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=clearledger
      - POSTGRES_USER=cl
      - POSTGRES_PASSWORD=${DB_PASS:-clearledger}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cl -d clearledger"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped

volumes:
  storage:
  pgdata:
  backups:
